name: todo_getx
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_svg: ^0.22.0
  get: ^4.3.8
  get_storage: ^2.0.3
  google_fonts: ^2.1.0
  receive_sharing_intent: ^1.4.5
  path_provider: ^2.0.14
  fteam_authentication_firebase: ^1.0.1+2

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
